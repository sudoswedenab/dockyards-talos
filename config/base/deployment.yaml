apiVersion: apps/v1
kind: Deployment
metadata:
  name: dockyards-talos
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: dockyards-talos
      app.kubernetes.io/part-of: dockyards
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dockyards-talos
        app.kubernetes.io/part-of: dockyards
      name: dockyards-talos
    spec:
      containers:
      - image: dockyards-talos
        name: dockyards-talos
        imagePullPolicy: IfNotPresent
        ports:
        - name: webhook
          containerPort: 9443
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
      serviceAccountName: dockyards-talos
      securityContext:
        fsGroup: 65532
        runAsUser: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
